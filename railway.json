{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install && pnpm --filter @fluxio/database db:generate && pnpm --filter @fluxio/web build"
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE"
  }
}
